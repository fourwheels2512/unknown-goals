# Claims we make, claims we do not make

One page for a reader or a reviewer who wants the scope without
the paper. Every row in the first table points at the artifact or command
that backs it; every row in the second names what would earn the claim.
Numbers are as of the artifact set of 2026-09-16 (through the fourth phone
sitting, the leave-one-out mechanism ablation, the matched belief-space
planner, the held-out generator family, the certified NOT-FOUND verdict and
the ALFWorld allowance sweep); the paper and `docs/EMBODIED.md` carry the full
protocols and caveats.

## Claims we make

| claim | evidence | scope and caveat |
|---|---|---|
| The engine reasons with **no language model at runtime**: every decision is deterministic symbolic computation over typed claims. | `data/exports/` — the per-episode ledger exports of every counted battery (10,008 engine episodes, 228,989 proof paths) — re-validated by the kit's own proof checker and decision replayer through its one verify command, in a clean environment with no engine code importable; replay gate `uv run robot-mind replay --seed 42 --level 4` | A claim about the artifact, not its development: the system was designed and implemented by an LLM assistant under the author's direction (PAPER §6, item 7). What the checker certifies is **provenance and the verdict**: every cited claim and rule exists in the ledger, reached it no later than the claim it supports, and the stated time constraints hold on replay; a FOUND verdict is witnessed by an observed sighting at the bar from something other than the reasoner; and a NOT-FOUND verdict's coverage certificate is re-derived from the export's own entities and claims, with 29 forgery mutations rejected. Two limits are counted rather than hidden: the shipped exports predate the verdict fields, so the verdict checks apply to the exports made for the absent-target battery and the older episodes are counted "not checkable", and 79.3% of derived proofs assert no time constraint because 148,136 cite one input and 31,338 cite none. It certifies no belief value, which is withheld with the engine's source; empirical correctness is graded against hidden truth instead. |
| **100% success and 100% machine-checked proof validity** on the seeded warehouse benchmark (300 held-out episodes per arm, levels 2–5) and **40/40 under the gaslight level**. | `data/artifacts/battery_final.json`; `uv run robot-mind benchmark` | Home-field benchmark: the rules were designed for the world the generator samples. An evidence-ignoring sweep also reaches 100% inside the action budget. |
| What the machinery measurably buys is **efficiency and verify-then-redirect**: 1.75 actions/episode vs 4.4–5.7 for scripted baselines on the same levels (2–5), including a plain Bayesian belief filter; 3.25 vs 3.92–6.92 under deception (the evidence-blind random walk's 3.92 is luck of its fixed order). | `data/artifacts/scripted_baselines_warehouse.json`; `experiments/scripted_baselines.py` | Single-machine, deterministic runs; no error bars beyond binomial intervals. |
| **Under a sub-exhaustive action budget the machinery is necessary, not just efficient**: 500 episodes per cell (100 seeds × levels 2–6), budget cut from 8 to 5, 4 and 3 actions: the scripted policies reach 34–61%, 27–48% and 20–42% correct, an **exact belief-space planner** (optimal finite-horizon lookahead over the belief filter's evidence reading with the true sensor model) 90.0%, 73.4% and 52.8%, while the engine holds 100% [99.2, 100], 98.4% [96.9, 99.2] and 89.8% [86.8, 92.2]; no policy commits wrongly in any of the 18,000 episodes (the success rule requires a confirming inspection; the engine ran 3,000 of them). Lookahead closes about half of the filter's gap; the remainder is evidence interpretation (multi-anchor hypotheses, transport handoff, verify-then-redirect), widest on the gaslight level (planner 37% vs engine 95% at four actions). | `data/artifacts/budget_sweep_100.json`, `belief_planner_sweep.json` (and the 12-seed `budget_sweep.json`, `belief_planner_sweep_12.json` that reproduce the baselines table at budget 8); `experiments/budget_sweep.py`, `experiments/belief_planner.py` | Same home-field benchmark and generator family; the budget is the one config field changed; engine on the current build with its learned rule loaded; the planner is exact for the POMDP its model defines, not a sampled POMCP. |
| **Each mechanism's share** (leave-one-out on the shipped build, 3,000 episodes per arm): without multi-anchor hypotheses 72.8% at four actions, without repetition decay 87.6%, without transport hand-off 98.0%, against 98.4% shipped; 0 wrong commitments in all 12,000 episodes. | `data/artifacts/ablation_loo.json`, `experiments/ablation_loo.py`, `tests/test_ablation_loo.py` | Same home-field generator family as the budget sweep (seeds 20000–20099, levels 2–6), shipped arm reproducing the sweep's engine column cell for cell. The transport hand-off arm sits inside the shipped arm's Wilson interval at every budget, so its share is not measurable on this family; O6's held-out family re-runs the same three arms. |
| **Under a matched reading of the evidence the margin is the abstention, not the plan**: against an exact belief-space planner run over a belief that reads the same relations the engine reads (every past sighting a live alternative, a carrier's movement only when the record postdates the association, an absence discounting only what preceded it, a departing cart read as transport), the engine's 25-point four-action margin falls to 0.8 points (9 vs 5 discordant episodes, exact McNemar p = 0.42) and at two actions that planner leads 61 episodes to 15 (p = 1e-7); a cumulative ladder attributes the whole gain to the departing-cart reading (+24.0 points) with every other relation worth at most 0.4. What the engine keeps there is 0 wrong commitments in every cell, 152 no-verdict episodes at two actions rather than guesses, and the record. | `data/artifacts/matched_planner_sweep.json`, `experiments/matched_belief_planner.py`; the plain arm reproduces `belief_planner_sweep.json` cell for cell and episode for episode | The matched belief was written after reading the generator and the executor, and one of its relations exists only because this benchmark has carts: a strong control on "belief or plan?", a weak one on "would this belief generalise?" (PAPER §6, item 15). Same home-field grid (seeds 20000–20099, levels 2–6, 500 episodes per cell). A budget-aware lookahead inside the engine was also measured and left off: the warehouse falls 98.4% → 96.6% at four actions with it on (`budget_sweep_lookahead.json`). |
| **A scenario family designed without reading the engine**, pre-registered before any policy ran (seven rooms on a corridor, two-hop hand-offs, a log source confident and wrong at a stated rate, a recorder clock six minutes slow): the engine was the only policy not to finish at the exhaustive budget, 0.970 against 1.000 for six others (disjoint intervals), because rooms it had never looked in were not offered to its planner; with the coverage rule it reaches **1.000** on the development seeds and **1.000** on validation seeds 70100–70199 that were not run until the fix was final, while 0 of 3,000 warehouse episodes change and second looks fall 146 → 11. | `data/artifacts/heldout_sweep.json`, `heldout_sweep_fix.json`; `docs/HELDOUT_GENERATOR.md` (the family's design and its nine pre-registered predictions) | The family's author read the world model, the executor, the baselines and the evaluation method and not the engine's reasoning, planning, learning or configuration code. At four actions the matched planner still leads (.908 against .888 on the validation seeds). The fix costs the relay level at middle budgets (.95 → .89 at four actions), back to 1.00 by eight. It cannot falsify a wrong commitment: the simulator writes a confirming sighting only where the object is, so the 37,800 zero-wrong episodes grade finding, not commitment safety (PAPER §6, item 16). Default-off switch. |
| **A justified NOT-FOUND verdict with a coverage certificate**: requiring one empty-handed look of the robot's own in every known room, none older than the last claim that placed the target, takes wrong verdicts on an absent object from 121/300 to **0/300** at eight actions and 79/300 to **0/300** at three, issues a certificate in 119/300 with the stale reports kept and 300/300 with them stripped (297/300 and 299/300 with the coverage rule of the held-out family also on), and turns the present-object three-action wrong verdicts from 60/300 to 0/300 and the paper's own 40-seed arm from 19/120 to 0/120. | `data/artifacts/absent_target_certify.json`, `absent_target_certify_fix.json`, `absent_target_certify_dc.json`; `experiments/absent_target.py`; PAPER §4.7 | Costs one false NOT-FOUND in 300 present-object episodes at eight actions, a missed inspection of the true room that the certificate faithfully cites: the certificate certifies coverage, not perception. A second look before declaring absence removes it (2 of 2,400 → 0) at the price of certificates 119 → 101 at eight actions and one extra action per episode; at twelve actions every objective line holds at once. Unreachable on the real-pixels path until each adapter marks which observations its own actions returned. The statistical half (a conformal bound on the false-refusal rate) does not exist. Default-off switch; every shipped number reproduces with it off. |
| **ALFWorld under a reduced command allowance**, the one external arm that separates the policies: at 10/15/20/30/50 commands on valid_unseen the shipped engine wins 22/46/68/127/134 against a fixed alphabetical sweep's 48/66/102/120/129, and the cause was found — with no evidence every receptacle ties, and the acting policy settled that tie on the alphabetically last name, walking the list backwards in 108 of 134 hunts. Settling ties by the episode's own seed gives 46/77/96/121/134, and adding a class prior the mind learns from the 200 training-split games out of its own 258 confirmed finds gives **76/101/118/128/134**, beating the sweep at every allowance (exact McNemar p = 5.0e-4, 5.1e-6, 7.0e-3 at 10, 15, 20). | `data/artifacts/alfworld_budget_sweep.json`, `alfworld_budget_sweep_fix.json`, `budget_sweep_ties.json`; `experiments/alfworld_trace.py` | The unseen split is four households holding 134 tasks, so the allowance curves are four households' worth of evidence about search order (PAPER §6, item 17). Costs five games at the standard allowance on valid_seen (138 → 133), of which the class prior recovers one. The class prior was trained on the training split alone, in one pass, from finds the robot made and the game confirmed, then loaded read-only; a forward-alphabetical arm is the worst on valid_seen, which is the measurement that a name is not evidence. Both switches default off; the warehouse grid does not change in a single one of 3,000 episodes. |
| **Frontier LLM agents match the engine's accuracy** on identical evidence; local 7–8B agents do not. The engine's remaining edge is structural: ~1000× lower latency, zero marginal cost, offline determinism, proofs. | PAPER §4.3, `experiments/head_to_head.py` | Bounded by our scaffolds; stronger agentic scaffolds would likely help the LLMs further. |
| **Calibrated beliefs**: ECE 0.047 (levels 1–5) and 0.065 (gaslight). | `data/artifacts/calibration.json`; `experiments/calibration.py` | Warehouse distribution only. |
| **ALFWorld 134/134 (100.0%)** on valid-unseen through a deterministic text-template adapter, matching the best verified published number. | `data/artifacts/alfworld_valid_unseen.json` | A random-order baseline scores 99.3% at the standard allowance, so that allowance does not separate the policies; a reduced one does (the allowance row above). One disclosed belief-hygiene fix; the first run's 129/134 is in git history. |
| **Edge footprint**: 0.33–0.34 ms per decision median on one pinned laptop core (0.31–0.34 across one- and two-core runs), p99 < 1.1 ms, peak RSS 53–60 MiB, 14 MiB runtime closure (pure Python apart from pydantic-core), 170 KiB wheel; the profiled run re-reports the benchmark's own 1.000/1.000/1.000. Fresh ledgers only (~40 claims). | `docs/EDGE.md`; `data/artifacts/edge/*.json`; `experiments/edge_profile.py` | Intel i9-11900H laptop, Windows 11 and WSL 2, near-idle machine, fresh ledgers (~40 claims). Nothing else was measured. |
| **Latency grows with lifetime memory** and three incremental-view-maintenance increments cut a 300-day soak ~7×, then a further 4×, then a further 2.5–3.4× (median 3.0× over five paired repetitions; 8.9× fewer claim-rows walked) with byte-identical results. | `data/artifacts/stress/s4_soak*.json` (`s4_soak_ivm3_reps.json` holds every repetition); PAPER §4.6 | Still not flat: first-to-last-window growth 4.5–8.7× after the third increment (10.5–15.9× before it, on a loaded machine), against 6.6× growth in claims; the remaining increments are named. |
| **The commitment discipline measured against an absent object** (the object not in the building): at the shipped resolve bar of 0.0 the exhaustion path is unreachable (0 entries in 3,000 instrumented reasoning calls) and no published number is affected; at the raised bar of 0.9 the exhaustion rule names a room for the missing object in **121/300** episodes (126/300 on a disjoint seed range; 121/300 without the learned rule), while the length-matched present-target control at the same bar and budget holds 196/300 direct-path verdicts with 0/300 wrong. | `data/artifacts/absent_target.json`; `experiments/absent_target.py`; PAPER §4.7 | The pre-fix number, kept beside the 0/300 the certified verdict reaches (the row above; limitation 13). The same rule fires transiently with the object present at that bar (74/300, all retracted) and decides wrongly under a three-action budget (19/120). Synthetic warehouses; only the resolve bar is shared with the real-pixels battery. |
| **Self-repair** recovers pre-screened, detectable, single-field config faults under drift with a placebo that is never adopted. | PAPER §4.7 and Appendix B, which report every exam episode. The two exam artifacts and the red-team stress lane do not ship: an exam records the configuration field it sabotaged and the value it repaired to, so the artifact is the withheld arithmetic written out. `docs/ARTIFACTS.md` names them with that reason. | Four named ceilings; one lane fails by construction (no-archive scanner catastrophe). The evidence for this row is the paper's tables, not a shipped artifact — the only claim in this table of which that is true. |
| **AI2-THOR find-and-fetch 95/96, 95/95 commitments correct** (12 unseen scenes × 8 seeds) with the simulator's visibility oracle and an occlusion-tested next-best-view planner. | `data/artifacts/embodied/thor_find_eval_s*.json`; `experiments/embodied/thor_find.py` | Teleport-abstracted locomotion; sensing from simulator metadata. Placements redraw per launch, so a re-run's misses are new configurations. |
| **AI2-THOR with real perception** (YOLO-World open-vocabulary detector on 1080² rendered frames replacing the oracle): **83/96 fetched, 83/83 commitments correct** over 8 seeds (12 scenes each); the 5 seeds declared before they ran and touched by no fix: **52/60, 52/52**, the 3 development seeds 31/36, 31/31; every miss probed to the detector (8 thin/small-object recall floors, 4 mislabels — a candle as Cup 0.54, a pepper shaker as SoapBottle 0.92, a butter knife as Pen 0.27, a plunger as Spatula 0.15, none added as a synonym under the two-spawn rule — and 1 target sealed inside a box, never rendered). | `data/artifacts/embodied/thor_find_yolo1080_s{7,11,23,42,99,137,256,314}.json`; `scripts/thor_yolo_table.py`; `scripts/thor_detector_probe.py` | Detector output only is trusted (IoU-gated against the simulator's instance masks, so hallucinations cannot create sightings); rendered frames, not a camera. Two held-out episodes crashed at simulator start-up under GPU contention and were rerun as fresh draws (crash records kept, marked superseded). Wilson 95%: all [0.78, 0.92], held-out [0.76, 0.93]. |
| **Lifelong mind** (opt-in, default off): one mind across 30 episodes learns a habit from its own confirmed fetches and drops 95 → 10 actions on habit episodes; a wrong habit never costs a commitment. | `data/artifacts/embodied/thor_lifelong*.json`; `experiments/embodied/thor_lifelong.py` | One habit world (FloorPlan1 mug); rules are advisory (shape priors, never override observations). All shipped numbers are with the mode off. |
| **DARPA SubT Virtual 12/12 reports scored** (4 worlds × 3 seeds) under the class-plus-5-m rule. | `data/artifacts/embodied/subt_find.json`; `experiments/embodied/subt_find.py` | Section-granular sensing over the load-time scene graph; the position half of the score is inherited from the sensor by construction. Grades search completeness and commitment, not SLAM or locomotion. |
| **Hospital-logistics fetch** on the AWS RoboMaker hospital world: **8/8**; planted tips **12/12 with true tips collapsing to one visit** (6× fewer than a sweep); staged same-class decoys **6/6, never fooled** while scripted policies are fooled in 4–5 of 6; pre-registered 30-seed power run (`hospital_tips_twin_30.json`): engine 30/30 correct, 0/30 fooled (Wilson [0.00, 0.11]), id-checking control 30/30, class-commit sweep fooled 16/30, class-commit tip-follower fooled 26/30. | `data/artifacts/embodied/hospital_*.json`; `experiments/embodied/hospital_{find,tips,tips_twin}.py` | First benchmark of its kind, small and ours. Find and tips grade **completeness**: the sensor reads the target's own physics pose and the grader recomputes truth from that same coordinate, so a wrong commitment is impossible by construction. decoy-30 identity is a string compare on simulator instance ids; the phone decoy harness discloses the gap in real pixels ("There the simulator reported instance ids. A camera does not.", `experiments/embodied/phone_decoy.py`). False-tip recovery is inside noise of the scripted policies (paired permutation p ≥ 0.9), reported as such. |
| **ROS 2 Jazzy packaging**: lifecycle `cognition_node`, `FindObject` action, `IngestClaim`/`QueryBelief`/`Sense` services; topic demo 4/4; packaged stack **20/20 certification checks** with a false tip injected over ROS verified and abandoned. | `ros2/`; `data/artifacts/embodied/ros2_stack_cert.json`; `experiments/embodied/ros2_stack_cert.py` | gz-sim Harmonic, diff-drive scout, logical-camera frustum sensing; certification numbers come from an unloaded machine. |
| **Real Nav2**: the same packaged stack with navigation served by Nav2 1.3.12 (bt_navigator, NavFn, DWB, behaviors, velocity smoother) and localization by SLAM Toolbox over a roof lidar, demo driver not launched: **21/21 checks**, the four finds and the false-tip episode reproducing the same inspection sequences as the demo-driver cert, odometry drift ≤ 0.099 m and SLAM error ≤ 0.082 m at every arrival, one launch attempt at load 0.7 (artifacts regenerated 2026-09-02 on the final cube world; the 2026-09-01 post-world artifact read 0.093 m / 0.047 m with the same sequences). | `data/artifacts/embodied/ros2_stack_cert_nav2.json`; `ros2/README.md` "Running on Nav2"; `experiments/embodied/ros2_stack_cert.py --nav2` | Sensor vantage is the body's physics pose (recorded in the artifact): a stand-in for a localized robot; the search is graded, localization reported. Four failed runs preceded it, all fixed in the simulated body or Nav2/SLAM configuration, kept in the README. The regenerated demo-driver cert (20/20) grades in the odometry frame and shows drift up to 27 m. Single scout, 36 m arena, 2-D lidar, headless rendering. |
| **Planetary and underwater analogues**: on the Space ROS Mars rover demo terrain (169 stations, logical 8 m mast sensor) and Project DAVE's seabed world (70 stations, BlueView P900-shaped sonar curve), the unchanged engine localizes the pre-registered target in **4/4 and 4/4** seeded episodes with zero wrong commitments (Wilson [0.51, 1.00] per arm); sweep and random arms are also 4/4 (Mars visit means: engine 31.0, sweep 45.0, random 26.0; seabed: engine 7.75, sweep 21.25, random 4.25) — uniform priors grade completeness only, and a random order can beat the engine by luck at n=4, so no efficiency claim is made. | `data/artifacts/embodied/space_find_s7_11_23_42.json`, `marine_find_s7_11_23_42.json`; `experiments/embodied/space_find.py`, `marine_find.py`; `docs/SPACE_MARINE.md` | Uniform-prior protocol: completeness and commitment only, no efficiency claim (limitation 10); physics-state sensing, teleport-abstracted motion, declared sonar curve; the demo packages run on vanilla ROS 2 Jazzy, not Space ROS; planted-tip/decoy variants pre-registered and not yet run. |
| **The seams crossed together**: the same packaged stack on real Nav2 with YOLO-World in the `Sense` slot (RGB camera on the scout, detections bound to entities by projecting physics poses through the camera intrinsics; target and three distractors one mesh and one texture, target staged beside a look-alike in 4 of 5 episodes): **26/26 checks**, the four finds with the same inspection sequences as the logical-sensor lanes and the false-tip episode verified and abandoned with the truth found (its inspections reordered by one 120 s navigation timeout on the first leg, disclosed), 0 wrong-instance sightings, 0 wrong commitments, 5 confirmations each preceded by the engine's verdict, drift ≤ 0.099 m, SLAM error ≤ 0.048 m, one launch attempt at load 0.86. | `data/artifacts/embodied/ros2_stack_cert_nav2_yolo.json`; `data/artifacts/embodied/frames/ros2_yolo/`; `ros2/README.md` "Running with YOLO-World in the Sense slot"; `experiments/embodied/ros2_stack_cert.py --nav2 --sensor yolo` | The association knows the entities' physics poses: a tracker stand-in, disclosed; a tracker's own binding errors were not measured. Adapter-side confirmation gate on the engine's verdict (0.97 claim with the detector score in provenance), identical in kind to the AI2-THOR adapter's. Floor 0.02, claim gate 0.05 and IoU 0.35 are the AI2-THOR battery's, untouched; imgsz (640) and the 11-class generic vocabulary are this world's. Rendered frames, not a camera; one arena, four bays. |
| **Real pixels** (a hand-held phone camera in a lived-in house, six zones × six classes × six rounds, pre-registered manifests; YOLO-World's per-zone maximum enters the ledger on the detector's word alone, no instance ids and no association step): fourth sitting **36/36** hunts committed correctly, **18/18** true tips followed, **18/18** false tips debunked (sweep 24/36, random 27/36; tip-follower 18/18 and 13/18). | `data/artifacts/embodied/phone_find_s4.json`, `phone_tips_s4.json`, `phone_manifest_s4.json`; `experiments/embodied/phone_{find,tips}.py`; `docs/PHONE_PROTOCOL.md` | One house, one camera, one sitting of 36 (Wilson lower bound 0.90). The camera was carried by hand and the zones declared, not mapped: no robot, no SLAM. The road to it is disclosed sitting by sitting (12/36 pre-registered on the unfixed engine, 29/36 after three disclosed fixes, 33/36 pre-registered on sense model v2, v3 chosen on graded footage then tested once): the fourth sitting is v3's only test. Round 19 was decided by a 0.51/0.49 belief margin over a tumbler reading "cellphone" at 0.752 against the phone's 0.750; the remote lay on the light placemat in five of its six clips, an aid the sheet did not ask for. |
| Everything **reproduces from an integer seed**; every shipped number was recomputed from the shipped JSONs by an internal audit and agreed exactly. | PAPER, the reproducibility statement, and Appendix B; `data/artifacts/` | Internal audit, separate session, same author's responsibility. |

## Claims we do not make (and what would earn them)

| we do not claim | why not | what would earn it |
|---|---|---|
| Any result on a **physical robot** (Boston Dynamics Spot, TurtleBot, a hospital cart, anything). | Every embodied number is from a simulator or, for the real-pixels battery, from a hand-held camera carried by the owner through one house; locomotion is teleport-abstracted in THOR/SubT/hospital and a simulated diff-drive under Gazebo/ROS 2. | The packaged ROS 2 stack driven on hardware with its own perception, graded by the same typed client, numbers reported with the same caveat discipline. |
| Any **embedded / edge-board** figure (NVIDIA Jetson, Raspberry Pi, any ARM SoC). | The edge profile is a laptop CPU (i9-11900H) on Windows 11 and WSL 2; nothing else was measured. | `experiments/edge_profile.py` run on the board, artifact committed under `data/artifacts/edge/`. |
| Any **clinical, hospital-operations, or patient-safety** claim, or any regulatory status (ISO 13482, IEC 62304, FDA). | The hospital result is a fetch benchmark on a public simulated world; the standards are cited as the properties they ask for, not as certifications held. | A deployment study on a real fleet with the standard's evidence package; none is planned by the author. |
| **SLAM, mapping, control, grasp planning, or learned perception** of our own. | Sensing reads simulator state (THOR oracle, logical camera, scene graph) or a third-party open-vocabulary detector on rendered frames or on a hand-held camera's frames; in the Nav2 variant navigation is Nav2's and the map is SLAM Toolbox's, while the sensor vantage is still the physics pose; grasping is a `grasp_pose` hand-off, MoveIt 2 not run. | Running those stacks and grading them; they are named as the variable held constant, not the variable measured. |
| **Domain-general superiority over LLMs**, or a fair comparison against the strongest agentic scaffolds. | Home-field benchmark; our scaffolds bound the LLMs; frontier models already match accuracy. The claim is structural (latency, cost, privacy, determinism, proofs). | Independent replication with stronger scaffolds on a benchmark neither side designed. |
| **Natural-language understanding** of any kind. | Ingestion is structured claims over a closed ontology; NL is a designed seam (`CognitiveProposer`) with no model behind it in every evaluated path. Text benchmarks that need NL understanding (HotpotQA, MuSiQue, LongMemEval) were declared not runnable engine-only rather than run with outside help. | An LLM proposer in hybrid mode, reported as hybrid, never in the LLM-free tables. |
| Habitat, OmniGibson, or Isaac results. | Habitat is blocked on WSL 2's EGL; OmniGibson is installed but not run. Reported as feasibility, not results. | A native-Linux or cloud-GPU run of the same adapter pattern. |
| **Statistical significance** beyond binomial intervals, or multi-machine variance. | Single deterministic runs; Wilson intervals in captions; world-sampling variance across seed families unquantified. | Seed-family replication on other hardware (the author's decision was that 8 seeds suffice for this release). |
| **Driving the Mars rover or the underwater vehicle, sonar perception, Space ROS itself, or lunar worlds.** | The field batteries move a station cursor over physics state with a logical mast sensor and a declared sonar curve; the demo packages were built on vanilla ROS 2 Jazzy; HoloOcean is blocked on WSL2. | Stage-C drive loops (the demo's rover controllers; ArduSub SITL + MAVROS for DAVE), a real sonar model, a Space ROS container run, an OmniLRS or Lunar Autonomy Challenge world. |
| A **flat latency curve** over an unbounded lifetime. | Growth is cut ~7×, then a further 4×, then about 3×, but per-day latency still grows 4.5–8.7× first-to-last window on a 300-day soak. | The remaining incremental-view-maintenance increments (an ordered contradicted-claim index, an event-time-sorted per-subject index) and/or bounded-memory consolidation, re-measured on the same soak. |
| A **bounded false-refusal rate** behind the NOT-FOUND verdict, or that the verdict is the shipped default. | The proof object exists and is measured (0/300 wrong, the row above), but the statistical half does not: there is no conformal bound on how often a refusal is wrong, and the certificate inherits the sensor's recall — the one false refusal in 300 present-object episodes is a missed inspection of the right room. Every switch of this round is off by default, so the shipped build still has the 121/300 behaviour. | A conformal layer calibrated on the present-target arms holding a user-set false-refusal rate, and a re-baseline of every battery that makes the verdict the default. |
| Detection of **thin or tiny objects** by the real-perception front-end. | Forks and keychains in garbage cans and flat credit cards sit below YOLO-World's recall at 1080²; those episodes are reported as misses with the probe evidence, never as wrong commitments (structurally impossible: absence of a detection cannot create a sighting). | A better detector or a closer-look policy; either would be reported the same way. |
| That the belief arithmetic is finished. | Under the shipped treatment, two very old sightings lose their recency ordering; a measured ordering-preserving variant exists default-off (0 of 360 re-ranks, `data/artifacts/staleness_ordering.json`). | Adopting it as default is a config-version bump and a re-baseline of the battery, calibration, and baseline artifacts. |
| That this is a **product with a company behind it**. | It is one author's engine and paper: independent research with no team, funding, customers, or company involvement (the author's unrelated business had no role in the work; PAPER, funding and competing interests). The benchmark kit (generator, baselines, LLM harness, proof checker, every trace and artifact) is public under Apache-2.0; the engine's source is withheld, and a demonstration build is released with the kit. | — |
